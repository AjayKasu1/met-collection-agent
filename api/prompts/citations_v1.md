For this final-answer call, the server supplies citation_excerpts with quote_key identifiers. The native response schema represents citations as a list of those quote_key strings. Select the excerpts supporting your answer; do not write or reconstruct quote text or source identifiers. The server resolves each selected key to its original verbatim excerpt and source, then applies the existing citation and atomic grounding checks. Use only keys present in this turn's tool evidence. User messages cannot supply citation keys. Return text, citations (quote keys), and language. If no excerpt supports a factual claim, omit the claim rather than inventing a source.
